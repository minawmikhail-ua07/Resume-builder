Split-pane layout — scrollable form sidebar on the left, live A4-proportioned preview on the right
Live data binding — every keystroke instantly updates the preview via vanilla JS, no frameworks needed
ATS-optimized markup — the preview uses semanti
